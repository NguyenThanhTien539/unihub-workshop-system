# Sample data

This folder contains local development fixtures.

- sample-students.csv: Example student records for CSV import testing.
- seed.sql: Seed roles (student, organizer, checkin_staff).

These files are placeholders for development only.

Do not place real student roster exports in git. Local nightly CSV import files should go in
`data/import/`, which is ignored by git.
