{
  "name": "unihub-workshop-system",
  "private": true,
  "scripts": {
    "mobile:start": "npm --prefix src/mobile run start",
    "mobile:android": "npm --prefix src/mobile run android",
    "mobile:ios": "npm --prefix src/mobile run ios",
    "mobile:web": "npm --prefix src/mobile run web",
    "start": "npm --prefix src/mobile run start",
    "web": "npm --prefix src/mobile run web"
  }
}
