import ScreenPlaceholder from "../components/ScreenPlaceholder";

export function OfflineSyncScreen() {
  // TODO: Add SQLite offline storage and sync queue handling.
  return (
    <ScreenPlaceholder
      title="Offline Sync"
      description="Placeholder for offline check-in storage and sync."
      todo="Store check-ins locally and sync when connectivity returns."
    />
  );
}
