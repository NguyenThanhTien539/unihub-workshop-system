import ScreenPlaceholder from "../components/ScreenPlaceholder";

export function CheckinSessionsScreen() {
  return (
    <ScreenPlaceholder
      title="Check-in Sessions"
      description="List available sessions and allow staff to start check-in."
    />
  );
}
