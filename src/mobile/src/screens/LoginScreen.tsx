import ScreenPlaceholder from "../components/ScreenPlaceholder";

export function LoginScreen() {
  return (
    <ScreenPlaceholder
      title="Login"
      description="Sign in flow for students, organizers, and staff."
    />
  );
}
